# WEB-FE-TESTER-PREPARATION-ARTICLE-v1.260

Status: WEB_VERIFY_PASSED. WEB_CLOSED takes effect only after reviewed commit, normal origin/main push with exact remote HEAD, verified full/delta/evidence ZIP/SHA256, clean archive validator and exact baseline+delta replay in external DELIVERY-CLOSURE.json.
Baseline: `11cb55424b1418a7f6136a0b8b2c77a8ddfa233c`.
Session: `S-LGO-WEB-20260916-E6A1`. Branch main; worktree `/Users/minhdc/Projects/LinhGioiOnline-Web` unchanged.

## Real Browser UI/UX Layout First

Scope is `/news/closed-tester-information-pack-started` only. The old production page clipped source summary, introduction and both chapter bodies in small proof cards. The new real page uses the already accepted illustrated editorial hero, original posting date, desktop contents column, mobile native contents disclosure and two full parchment chapters. Existing Public Core palette, framing and artwork plus unchanged common navigation are the comparison target. The target was viewed and the actual page reviewed desktop/mobile. No new generated design, screenshot-as-interface or pixel-identical atlas claim.

The original title Gói thông tin người kiểm thử đã sẵn sàng, summary, body, publishedAt and both contentDetailSections records are preserved. Every heading/body/playerImpact/nonClaim remains complete. Ngày đăng nội dung displays05/09/2026 from2026-09-05T23:00:00.000Z through the existing vi-VN/UTC formatter; it is not a test invitation or release date. The source is historical WEB v1.20 publication content, not evidence of currently available tester recruitment. Attached historical game/runtime documents do not replace this independent Web source or authorize game/backend work.

Seven contextual links improve the actual reading flow. Chapter1 leads directly to the existing tester checklist, device-report guidance and known limitations. Chapter2 leads to the existing feedback template, full preparation guide, safety guidance and FAQ. Three related source articles keep their original selection/order, titles and complete summaries. No duplicate checklist, registration form, template editor, recruitment workflow or permission grant is added to the article.

## Real destination interaction

Four new integration cases per browser profile activate the article links by keyboard and verify the target section receives focus below the sticky header. Tab reaches a real control inside the destination. Space checks the first checklist item; Enter opens the device/limits native disclosures; End selects the existing Góp ý feedback tab. Reload returns checklist choices to their unselected local state and restores the requested section focus. Back returns to the original article. Requests are observed during local interactions: no fetch/XHR/non-GET submission. The source of PublicTesterPackExperience and the preparation guide is unchanged; this task connects to and tests existing tools, not a new data-collection service.

Article contents, previous/next, return-to-contents, direct URLs and Back/Forward use unchanged shared fragment/focus behavior. Invalid fragments do not seize chapter focus; decorative-image failure leaves content readable. At320px the source chapter text is at least14px, visible links/disclosures at least44px high, and normal/forced-colors focus is tested. Reduced-motion arrival is immediate. Live resize across desktop, portrait, landscape and tablet preserves one contents navigation and all text. Increased line/letter/word spacing does not clip source fields or hide the final FAQ link. In-page reading leaves local/session storage unchanged.

## Base First and code ownership

PublicTesterPreparationArticle is only a typed source/label/link composition of existing PublishedArticle. All packages, CSS, tokens, artwork/fonts, fixture/contract data, dependencies/lockfile, shared shell, Portal/Ops and previously delivered article wrappers remain unchanged. The selected-slug branch follows the existing category/notFound guard and related selection. The generic renderer serving10 remaining news articles is byte-for-byte unchanged. Its shared CSS remains in use and was not deleted just to report cleanup.

Two historical compact tests/validators target this exact slug: v1.183 and v1.196. Both are HISTORICAL_SUPERSEDED, not counted as runtime PASS. Only their obsolete small-card layout requirements are replaced. Existing other-page assertions are unchanged. The new source guard consumes the established shared article contract and checks the exact selected source, labels and all7 links including4 fragment IDs.

## Verification and visual evidence

Test-first:18 desktop cases failed on the old production page before implementation. After the composition,86/86 focused dev cases passed, including36 for this article. Component21/21, next-route13/13, four typechecks, two lints and62 raw dev-DOM/geometry comparisons passed. These dev results do not substitute for production evidence.

One fresh production build passed with63 static pages. The verified owned production listener was stopped with SIGTERM only after matching PID, parent, start-time, cwd, launch command and port claim; its parent runner ended naturally. The same3236 port was reused, not a new per-version port. A browser probe confirmed the new article, two chapters and7 links, old compact renderer absent, HTTP200 and no JavaScript errors. Dev3221 was not restarted or stopped.

Fresh selected production regression:774/774 PASS with zero failures/skips/flaky results across64 files. The new article contributes36 tests, including8 tester-destination cases. Both historical duplicate compact files were removed from selection. All297 runtime input hashes are checked before commit/delivery. No repeated production build for documentation-only changes.

| Production viewport | Height | Hero bottom | Article top | Related columns | Overflow | Main axe violations |
|---|---:|---:|---:|---:|---:|---:|
| 1440 × 900 | 2917 | 633.59 | 665.59 | 3 | 0 | 0 |
| 1280 × 800 | 2878 | 625.19 | 657.19 | 3 | 0 | 0 |
| 768 × 1024 | 3779 | 842.62 | 866.62 | 1 | 0 | 0 |
| 390 × 844 | 5005 | 1013.75 | 1037.75 | 1 | 0 | 0 |
| 360 × 800 | 5171 | 1050.91 | 1074.91 | 1 | 0 | 0 |

Five production viewports were captured as first fold, full page and second-chapter arrival. No horizontal/heading overflow, page JavaScript error, missing cover or main-content axe violation in selected tags was observed. This is Chromium viewport emulation and scoped axe, not physical-device/screen-reader testing or WCAG certification.

Thirty-one unchanged surfaces use fresh production baselines from v1.259 captured before editing:62 exact raw main-DOM matches and62 exact measured geometries. They include the six delivered article pages, news/guide libraries and actual tester-tool page. No generated-ID-only exception was needed. No baseline was recaptured from new source and no geometry tolerance increased. Dev captures are retained separately.

Two setup mistakes were diagnosed without hiding failures: the first capture script retained the previous article's navigation accessible name and timed out before mobile capture; only its exact locator name was corrected and the failed log retained. A premature mobile-image read returned ENOENT, not a generated artifact. A later prebuild script read the production compatibility filename while only the explicitly dev-suffixed result existed; it stopped before any signal/build. The correct dev result was then verified. Neither correction changed UI, relaxed assertions or repeated a sensitive command.

After report/state, final source gate: UI/Web/Portal/Ops typecheck, UI/Web lint, published-frame4 + article4 + reading6 + announcement7 component tests, next-route13, clean-source current-state and git diff --check. Nineteen negative controls reject wrong source selection, altered source fields/date/related links, broken shared bindings, guide defaults/focus/reduced-motion, data intake and each of the4 broken tester destination fragments. Exact command exits are closure-checks.json. Code/browser review is inline; no independent reviewer-agent claim.

## Coordination, delivery and limits

The previous v1.259 package checksum entries were freshly verified. No new pause/recovery/inbox or active worker job was present. The old v1.255 dashboard task stays under operator REVIEW and is not repurposed or silently completed. PID-capacity waiver is limited to local own-command/PID journaling; new pause/recovery/ownership checks remain active. No Manager configuration, SQL state, bridge/client credentials or other-session resources were accessed or changed.

Fixed runtime roles are dev3221 and production3236. No old per-version server was recreated; no artifact was deleted. The new handoff-directory write claim is temporary and releases only after ZIP/SHA/replay verification and no open handles. Final state is WAITING_USER with only the5 long-lived claims. A local preview listener is not background assistant work.

Only reviewed source/tests/docs are committed, followed by normal push, exact remote HEAD, full/delta/evidence ZIP without parent wrapper, SHA256 and exact replay. No force-push, source restore or production deployment. No new auth, DB persistence, CMS, backend, sensitive-data intake, tester recruitment, entitlement, download artifact or live-ops claim. NO_ACCEPTED_BACKEND_CONTRACT.
Whole-app JavaScript-disabled streaming remains open; native controls do not establish universal no-JavaScript rendering.

Next only after delivery: WEB-FE-ACCESSIBILITY-INTERACTION-AUDIT-v1.261, `/news/community-roadmap-onboarding-started`; not implemented here.
