# HANDOFF — WEB-FE-STORY-VISUAL-REALIGNMENT-v1.267

`/story` has been rebuilt from the historical generic proof/disclosure layout into its page-specific immersive target composition. Fresh focused20/20 PASS, selected dev60/60 PASS, production60/60 PASS and build63 are recorded; desktop/mobile screenshots were visually reviewed after real RED→GREEN corrections.

Authority is `PublicStoryLanding` + `story-landing-layout.css` + shared `immersive-shell.css`, with canonical `narrativeChapters` data. Six source-derived art crops are hash/provenance locked. Historical story layout validators v1.64/v1.84/v1.121/v1.136/v1.214 are superseded, not runtime PASS.

Implementation source commit: `f631e2aa129b367977ff301e725fd4cb29b2f541`, verified on `origin/main`. Final governance HEAD and archive/replay SHA evidence are produced by the v1.267 delivery package. No fake trailer/live event/download/login/backend claim.

Next: `/classes` visual realignment v1.268. Continue one page at a time; do not reopen `/story` without a related regression and do not resume the historical `/news` queue.
