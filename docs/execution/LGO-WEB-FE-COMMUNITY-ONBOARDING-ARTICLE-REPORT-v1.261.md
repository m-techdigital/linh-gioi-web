# WEB-FE-COMMUNITY-ONBOARDING-ARTICLE-v1.261

Status: WEB_VERIFY_PASSED. WEB_CLOSED takes effect only after reviewed source commit, normal origin/main push with matching remote HEAD, full/delta/evidence ZIP integrity/SHA256, clean extracted source validation and exact baseline+delta replay are verified in external DELIVERY-CLOSURE.json.
Baseline: `ced6b736518f32aeec32c5364f74829496823ae9`.
Session: `S-LGO-WEB-20260916-E6A1`. Same main and `/Users/minhdc/Projects/LinhGioiOnline-Web`.

## Recovery before continuation

The preceding chat displayed thinking failure. The first actions were read-only reconciliation, not rerunning writes. Actual local/remote HEAD was already ced6b73; v1.260 delivery manifest, test logs, push and packaging evidence were present. Seven package checksum entries were freshly matched and previous test/commit/push/package PIDs were observed ended. No v1.260 command was repeated and source was not restored. The old774/774 result belongs to v1.260, not this task. The current WEB-NEXT-ACTION selected this v1.261 article. No pause/recovery/inbox instruction/active worker job blocked the task. The unrelated v1.255 dashboard task stays operator REVIEW.

## Real Browser UI/UX Layout First

Only `/news/community-roadmap-onboarding-started` is redesigned. Its former production UI clipped introduction and chapter copy in small proof cards, without the current article reading layout. The new page uses the existing illustrated editorial header, source posting date, desktop contents column and two full parchment chapters. Mobile has one reading column and a native contents disclosure that can close without hiding article sections. The accepted Public Core palette, typography hierarchy, artwork and framing are comparison guardrails; the shared header/footer/navigation remain unchanged. Real rendered HTML and desktop/mobile screenshots were reviewed; an image of a design is not a substitute for the page. Pixel-identical atlas artwork is not claimed.

The original title Onboarding cộng đồng và roadmap rõ hơn, summary, body, publishedAt and both contentDetailSections records are preserved. Every heading/body/playerImpact/nonClaim is shown in full rather than ellipsized. Ngày đăng nội dung is05/09/2026 using the established vi-VN/UTC formatter, not a game launch date. The original WEB v1.11 text remains a historical description; this implementation does not turn it into current game-operation evidence. Attached historical game foundation, database and auth reports are not substituted for the current independent Web source.

Eight contextual links join the reading journey. Chapter1 opens Home, Community, Community Onboarding and Safety. Chapter2 opens Roadmap, Status, Download Trust and Release Readiness. Each was activated by keyboard and returned from with Back in both browser profiles. These are existing informational destinations, not enrollment, publishing, approval or download commands. Breadcrumb returns to News. Three related articles retain original selection/order, full titles/summaries and working links.

In-page contents, previous/next, return-to-contents, direct fragments and Back/Forward use unchanged shared fragment/focus behavior. The requested section arrives below the real sticky header. Unknown/malformed fragments do not seize focus. Missing decorative artwork leaves complete chapters and keyboard navigation usable. At320px source paragraphs are at least14px and visible links/summary controls at least44px high, with normal/forced-colors focus. Increased line/letter/word spacing and live resizing through desktop, portrait, landscape and tablet retain all source fields and one coherent contents navigation. Reduced-motion arrival is immediate.

## Existing destination interactions verified

From the new article, the real Community Onboarding page is opened and all three existing steps are selected using keyboard Space. The selected control keeps focus, exactly one step is current, visible position changes1/3 through3/3, and each step retains its original Status/Roadmap/Community destination. Boundary buttons stay disabled appropriately and reload returns to the first local step. Requests and storage are compared: no enrollment submission or reader persistence. The boundary continues to say Chưa có danh sách chờ. The article does not duplicate the journey control or its state.

The Roadmap destination is also opened from the article. All source decision gates and staged-release entries are counted against roadmapDecisionGates and stagedReleaseMessages. Enter/Space opens each native proof disclosure, exposing the original decision owner, restrictions and stage copy. data-state and data-visibility remain equal to source before and after interaction; reading does not approve a gate or open a stage. No request is submitted and no download form/link is fabricated. Destination screenshots are retained with tests. Four browser cases cover these two paths on desktop/mobile, in addition to28 article cases.

## Base First and code ownership

PublicCommunityOnboardingArticle is a thin source/labels/links composition of unchanged PublishedArticle. No new article markup, controller, stylesheet or duplicated destination tools. Every packages file, CSS file, theme token, artwork/font, source fixture/contract, dependency/lockfile, shared shell and prior article wrapper remains unchanged. Community, Onboarding and Roadmap production owners are unchanged. The selected-slug branch follows the existing category/notFound guard and related-source selection. The generic return still used by9 other news articles is byte-for-byte unchanged; its CSS remains needed and was not removed simply to report cleanup.

Only obsolete compact article v1.188 test/validator is HISTORICAL_SUPERSEDED, not runtime PASS. Other selected browser assertions remain intact. The new source guard consumes the established shared article contract and exact selected-source bindings, date semantics, labels and eight destinations. Code/browser review was inline; no independent reviewer-agent result is claimed.

## Runtime and visual evidence

Test-first:16 desktop cases executed and failed on the old production page. After implementation,90/90 focused dev cases passed across this article, the delivered tester article and News library. Component21/21 across four groups, next-route13/13, four typechecks and two lints passed. Fresh dev-before/dev-after comparisons matched68 raw DOM/geometries. No production claim was made from these dev results.

One new production build passed with63 static pages. The fixed3236 listener was reused with the identity-checked lifecycle below. A real browser probe confirmed HTTP200, new article1, old compact0, two chapters, eight links and no page errors on that production build. All298 runtime input hashes are rechecked before delivery. Dev captures are archived separately; the final after images come from production, not dev.

The planned selected regression was enumerated before execution:804 tests in64 files. Fresh production execution matched804/804 PASS with zero failed/skipped/flaky. This is a selected regression suite, not a claim about every historical test in the repository.32 cases belong to the new article, including4 tests of actual onward interactions. Test inventory, commands, JSON report, trace and screenshot evidence are in handoff/continuous-v1.261/evidence.

| Production viewport | Page height | Hero bottom | Article top | Related columns | Overflow | Main axe violations |
|---|---:|---:|---:|---:|---:|---:|
| 1440 × 900 | 2954 | 633.59 | 665.59 | 3 | 0 | 0 |
| 1280 × 800 | 2917 | 625.19 | 657.19 | 3 | 0 | 0 |
| 768 × 1024 | 3751 | 842.62 | 866.62 | 1 | 0 | 0 |
| 390 × 844 | 4965 | 1050.91 | 1074.91 | 1 | 0 | 0 |
| 360 × 800 | 5120 | 1076.83 | 1100.83 | 1 | 0 | 0 |

No document/heading overflow, missing cover, page JavaScript error or scoped main-content axe violation was observed at five production viewports. These are Chromium viewport simulations and selected axe tags, not physical-device/screen-reader testing or WCAG certification. Article reading itself sends no fetch/XHR/non-GET request and changes no local/session storage.

Thirty-four unchanged surfaces use fresh v1.260 production baselines captured before editing. All68 main-DOM and measured geometry comparisons match exactly, including the delivered articles, News/Guide libraries and Community Onboarding/Roadmap destinations. No generated-ID exception was needed in the closing result. Existing narrow generated-ID comparators for /performance and /guides still require resolved unique references and reject changed visible text; no new normalization or geometry tolerance was introduced. Baselines were not recaptured from new source.

After report/state, the final source gate checks UI/Web/Portal/Ops types, UI/Web lint, published-frame4 + article4 + reading6 + announcement7 component tests, next-route13, clean-source current-state and git diff --check. Seventeen negative controls reject wrong selection, rewritten original fields/date/related links, broken shared bindings/focus/reduced-motion, intake and altered Onboarding/Roadmap destinations. Exact command outcomes are closure-checks.json; source validation does not replace browser evidence.

## Coordination and fixed runtime lifecycle

Only five long-lived own claims are retained: this worktree, main, the Next build runtime, dev3221 and production3236. No new port per version. Before SIGTERM, the existing3236 process was checked against preflight PID/parent/start-time/command/cwd, the previous own launch journal and its claim. Only that exact listener received SIGTERM. Its parent runner ended naturally. No process-group signal, forced kill, dev restart or other-session action. The new build runs on the same3236 role, not a new port; the listener is a user preview, not ongoing background assistant work.

Pause/recovery/ownership checks remain active; PID capacity waiver uses local-process-journal.jsonl, not Manager edits. No SQL state changes, bridge/client-secret access, branch/worktree switch or source restoration. The prior v1.260 completion was reused only after actual recovery evidence. The unrelated dashboard task under operator REVIEW was not silently closed or repurposed.

Only reviewed source/tests/docs enter Git. Normal push and exact remote HEAD plus full/delta/evidence archive integrity, checksum, clean archive validator and exact baseline+delta replay precede closure. The handoff write claim is temporary and released after verification and no open handle; the ZIP/evidence are retained. Final registry state is WAITING_USER after verified delivery. No force-push, production deployment or backend mutation.

No fabricated community backend, waitlist, account, gate approval, release date, download entitlement, notification or CMS. No independent backend. No production auth. No DB persistence. NO_ACCEPTED_BACKEND_CONTRACT. Whole-app JavaScript-disabled streaming remains open; native controls do not prove universal no-JavaScript rendering.

Next after verified delivery only: WEB-FE-ACCESSIBILITY-INTERACTION-AUDIT-v1.262, `/news/content-ia-hub-polish-started`; not implemented here.
