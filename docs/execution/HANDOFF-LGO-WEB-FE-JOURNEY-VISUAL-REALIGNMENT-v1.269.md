# HANDOFF — WEB-FE-JOURNEY-VISUAL-REALIGNMENT-v1.269

`/journey` has been rebuilt from the historical generic hero/proof-board flow into its page-specific immersive 20-minute journey composition. Focused12/12 PASS, selected dev86/86 PASS, production86/86 PASS and build63 are recorded; BEFORE/MID/AFTER desktop/mobile screenshots were visually reviewed.

Authority is `PublicJourneyLanding` + `journey-landing-layout.css` + shared `immersive-shell.css`, with canonical `sampleSessionBeats` and `worldRouteStops`. One clean Linh Thành hero crop is target-derived and hash/provenance locked; session/route imagery reuses the already audited world artwork.

Implementation source commit: `1b3e9c98fcd43981867f764b89272783a262c013`, verified on `origin/main`. Historical journey board/target/layout suites v1.80/v1.123/v1.138/v1.216 are superseded, not runtime PASS. Canonical content, v1.22/v1.25 and public navigation guards remain active.

No live session telemetry, saved progression, loot grant, account/guild mutation, playable backend, download entitlement, login or production deployment is claimed.

Next: `/start` visual realignment v1.270. Continue one page at a time; do not reopen `/journey` without a related regression and do not resume the historical `/news` queue.
