# WEB-FE-FAQ-GUIDE-ARTICLE-v1.250

Status: WEB_VERIFY_PASSED. Effective WEB_CLOSED requires the external delivery manifest to verify reviewed commit, normal origin/main push, matching remote HEAD, full/delta/evidence ZIP integrity, SHA256 and exact source replay.
Baseline: `eed4d8a96b3f9241330fe29277095fbaa98d9b5d`.
Session: `S-LGO-WEB-20260916-E6A1`. Existing main and `/Users/minhdc/Projects/LinhGioiOnline-Web` retained.

## Real Browser UI/UX Layout First

Scope: `/guides/faq-search-helpfulness-guide`. The previous page had four clipped cards and repeated unrelated CTA bands. The new page is a framed editorial article: four source-linked overview rows, responsive contents, four complete parchment chapters and a short related-reading area. The accepted Public Core navy/gold/jade typography/frame/shell is the comparison target, registered for this exact route. No screenshot or design image substitutes for HTML controls; the article adds no illustration request. Pixel-identical atlas illustration reproduction is not claimed.

The source title, summary, body and all four ordered title/action/expectedResult/blockedScope records are consumed unchanged. Source wording about FAQ topics, static guidance, search boundaries and feedback remains intact. No historical game attachment is used to replace the current Web fixtures or infer backend readiness. The actual FAQ directory retains its existing six labels; the guide does not invent new filters to reconcile older source terminology. No content is line-clamped to force a shorter page.

The linked overview is the existing ReadingPriorityPanel, generated from the same four steps as the article. It is chapter navigation, not search results, a chatbot or submission progress. Native previous/contents/next, Tab/Enter/Space, Back/Forward, direct chapter URLs and malformed fragments are tested. At narrow widths the contents disclosure and chapter action groups remain usable.

Nine chapter actions serve the authored instructions. Chapter 1 opens `/support/help#faq-answers`. Chapter 2 retains all five destinations in the authored order: Support, Safe Support, Download Trust, Status and Tester Pack. Chapter 3 opens `/support/help#help-boundary`. Chapter 4 opens the existing safe feedback template and privacy guidance. The article contains no input form or second search/filter controller; only the existing FAQ directory owns category selection.

## Required destination fix and actual interactions

Before changes, a cold FAQ answer-index URL already restored focus correctly and ordinary/unknown/malformed URLs passed their control. The privacy/search-boundary URL failed because its aside was not focusable and had no mount-time fragment restoration. Only that required target now opts into tabindex=-1 and the existing single-ID ArticleFragmentRestoration helper. One shared focus selector includes opt-in paper panels. No directory filtering, hash/history state, FAQ content, questions, grouping, buttons or root scrolling implementation was changed.

End-to-end tests navigate from this article to the existing FAQ index, measure its position below the actual sticky header, Tab to All, activate each of the six category buttons and confirm one visible group plus the actual 1/6 announcement. All restores six groups. Native Enter/Space expands a complete source answer and its onward link opens Download Trust. Category interaction retains focus and issues no fetch/XHR/non-GET request. This is local category selection, not newly implemented keyword search, autocomplete, ranking or backend indexing.

The boundary link now arrives visibly below the header, focuses the aside and hands Tab to its real privacy link. Cold URLs and malformed controls are tested. The feedback route reaches the already-fixed tester template; Tab/End operate the existing tabs without clipboard writes or user-data intake. Feedback submission is not implemented: authored guidance about preparing feedback is not a claim that messages were sent.

## Base First and scope review

ExperienceHero, ReadingPriorityPanel, GuideArticle, GuideChapterBody, grouped actions, buttons, QuestionDirectory and fragment restoration are reused without implementation changes. All 50 obsolete FAQ-guide-only CSS lines are removed, with every remaining service-layout.css byte equal to baseline. The only shared CSS difference is an extra focus-selector arm. Globals, tokens, assets/fonts, dependencies, content/contracts, site shell, common article/directory code, TesterPackExperience, Portal and Ops are unchanged. No new stateful or service owner.

Only this exact slug is specialized after the published/category guard; dynamicParams remains false. The multi-guide HTTP/source test now checks this last compact guide explicitly. Only its obsolete v1.173 compact validator/E2E is HISTORICAL_SUPERSEDED, not counted as PASS. URL/category/heading/overflow and actual source checks remain active. Inline source/browser review was performed; no independent reviewer-agent result is claimed.

## Verification

RED: eleven new article tests and the cold help-boundary test failed against the baseline. Two existing-behavior controls passed (cold FAQ index, ordinary/unknown/malformed URLs). GREEN: 40/40 across the new guide, destination and existing FAQ suite. One final production build passed with 63 static pages. Fresh production regression after the final source: 496/496 PASS, zero failed/skipped/flaky, across 41 selected E2E files. The new article contributes 22 tests and destination suite 6. All 284 build input hashes are pinned and rechecked at source closure/delivery.

| Viewport | Document height | Hero bottom | Article top | Horizontal overflow | Main axe violations |
|---|---:|---:|---:|---:|---:|
| 1440 × 900 | 3534 | 659.14 | 691.14 | 0 | 0 |
| 1280 × 800 | 3513 | 659.14 | 691.14 | 0 | 0 |
| 768 × 1024 | 4147 | 1001.62 | 1025.62 | 0 | 0 |
| 390 × 844 | 5417 | 1169.28 | 1193.28 | 0 | 0 |
| 360 × 800 | 5632 | 1215.34 | 1239.34 | 0 | 0 |

All five production captures have zero page JavaScript errors, heading overflow and image requests for this article. This is not a zero-network website claim. At 320px, tests require body >=14px, chapter actions/navigation >=44px and visible focus. Axe scans main content with selected WCAG tags; Chromium emulation is not physical-device, screen-reader or WCAG certification.

Compatibility across 18 sibling pages: 34 raw exact main-DOM matches and 2 generated-ID-only matches; all 36 measured geometries match. The pre-existing `/performance` exception uses a bijective generated-ID spelling normalization with unique/resolved labels and a negative altered-copy control. Normalized DOM is not called byte-identical. FAQ is compared separately: both default-view geometries match, allowing only the intended help-boundary tabindex and generated-ID spelling. Altered visible copy is rejected; ordinary scrollY remains zero.

Final source checks run after this report/state update: UI/Web/Portal/Ops typecheck, UI/Web lint, seven next-route tooling tests, clean-source current-state validation and git diff --check. Eleven negative controls reject altered authored outcomes, wrong answer/boundary/feedback/support destinations, invented service wording, broken overview links, missing boundary restoration/focus/style and unexpected dynamic routes. Actual exits are recorded in closure-checks.json rather than inferred from this report.

## MCP, delivery and boundaries

Owner waiver for stored-PID capacity remains; command/PID metadata is recorded in local-process-journal.jsonl. Pause, ownership, worktree/runtime/port/branch checks and evidence/status updates remain active. Already-enrolled v2 status showed no assigned task/batch/run. No client identity is copied; no Manager/registry cleanup/config change, process kill/restart or other-session change. New production preview is on own claimed loopback port 3228; a listener is not background assistant work.

Reviewed source/test/docs only enter Git. Handoff artifacts, screenshots, cache and secrets stay out. Normal push, matching remote HEAD, verified ZIP/SHA256 and baseline+delta replay precede effective closure. No force-push or deployment command.

No online search/index/ranking/chatbot, personalized history, ticket/account lookup, feedback intake, production SLA, login, test signup/access, payment/economy or game backend. No production auth. No DB persistence. No independent backend. No CMS. No production deployment. NO_ACCEPTED_BACKEND_CONTRACT.
Whole-app JavaScript-disabled streaming remains open. This is not a universal fragment fix or a claim about current game capabilities beyond unchanged Web fixtures.

After verified delivery only: WEB-FE-ACCESSIBILITY-INTERACTION-AUDIT-v1.251, `/events` (existing event page; no event system or live schedule implied).
