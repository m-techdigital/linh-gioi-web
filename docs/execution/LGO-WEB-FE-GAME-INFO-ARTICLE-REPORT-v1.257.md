# WEB-FE-GAME-INFO-ARTICLE-v1.257

Status: WEB_VERIFY_PASSED. WEB_CLOSED becomes effective only after reviewed commit, normal origin/main push with matching remote HEAD, verified full/delta/evidence ZIP integrity/SHA256 and exact baseline+delta replay in external DELIVERY-CLOSURE.json.
Baseline: `010db2c76a08959532bce7ccb2f8bbc4b418daff`.
Session: `S-LGO-WEB-20260916-E6A1`; unchanged main and `/Users/minhdc/Projects/LinhGioiOnline-Web`.

## Real Browser UI/UX Layout First

Only `/news/public-game-info-depth-started` is redesigned. The prior compact article cut its introduction, section bodies and player impacts inside proof cards. The new page uses the accepted shared illustrated editorial hero, original posting date, two complete parchment chapters, desktop contents column and mobile native contents disclosure. Public Core palette, illustration, framing and unchanged common shell are the design guardrail. The actual rendered HTML was reviewed; no design screenshot replaces page UI and no pixel-identical atlas claim is made.

The original title Thông tin game public được làm sâu hơn, summary, body, publishedAt and both contentDetailSections records are consumed unchanged. Every chapter heading/body/playerImpact/nonClaim is readable without clipping. Ngày đăng nội dung shows 05/09/2026, from the original vi-VN/UTC timestamp. The article is historical website information, not an announcement of current game-server availability, statistics, accounts or quests. The game foundation reports attached in the conversation are not substituted for this independent Web source.

Seven contextual destinations make the source's reading journey actionable: Story, Classes and Beginner Guide in the first chapter; Download conditions, FAQ, Community Onboarding and Status in the second. Every link was opened using keyboard and returned from with Back in both browser profiles. These are existing information pages, not registration, data intake or game entitlement. Breadcrumb returns to News; related selection remains the first three other published articles with original titles, order, summaries and actual links.

Two native contents links, previous/next and return-to-contents use real section IDs and place focus below the sticky header. Direct fragment URLs, Back/Forward, malformed/unknown fragments, missing decorative artwork and reduced-motion arrival were tested. Closing the native contents disclosure does not hide article chapters. At320px source paragraphs are at least14px and visible links/summary controls at least44px high. Forced-colors focus is tested. Live reflow through desktop, portrait, landscape and tablet preserves the single contents structure, source text and proper related-card columns. Expanded line/letter/word spacing does not clip paragraphs or hide the onward link.

## Base First and scope control

PublicGameInfoArticle is only a source/label/link composition of PublishedArticle. PublishedArticle, GuideArticle, GuideChapterBody, GuidanceTopicGrid and every packages file are unchanged. No duplicate article markup, CSS pattern, filter, device control or navigation state was introduced. The exact selected slug branch comes after the original category/notFound guard and related selection. The generic return used by the13 remaining news articles is byte-for-byte unchanged.

Zero CSS added, removed or modified. Generic news CSS remains necessary for13 consumers. No globals.css, tokens, artwork/fonts, source fixtures/contracts, dependency/lockfile, shared shell, Portal/Ops, game/backend changes. Only the selected route is registered against the existing Public Core comparison target; the artwork itself is not regenerated.

## Runtime and visual evidence

Test-first:14 desktop cases failed against the real old production page before implementation. GREEN:104/104 targeted browser cases across the new page and three previously delivered articles. One new production build passed with63 static pages. Fresh selected production regression:690/690 PASS, zero failed/skipped/flaky across65 files. The new article contributes28 tests; only the exact old compact v1.180 article test/validator was superseded and is not counted as runtime PASS. Other previously selected suites remain active and their browser assertions are not relaxed.

| Production viewport | Page height | Hero bottom | Article top | Related columns | Overflow | Main axe violations |
|---|---:|---:|---:|---:|---:|---:|
| 1440 × 900 | 3003 | 633.59 | 665.59 | 3 | 0 | 0 |
| 1280 × 800 | 2964 | 625.19 | 657.19 | 3 | 0 | 0 |
| 768 × 1024 | 3798 | 842.62 | 866.62 | 1 | 0 | 0 |
| 390 × 844 | 5079 | 1055.08 | 1079.08 | 1 | 0 | 0 |
| 360 × 800 | 5207 | 1076.83 | 1100.83 | 1 | 0 | 0 |

Five production viewports cover the first fold, full page and second-chapter arrival. No heading/document overflow, page JavaScript error, missing cover or detected main-content axe violation in selected tags. Native in-page reading sends no fetch/XHR/non-GET request and does not change local/session storage. This is Chromium emulation with scoped axe, not physical-device, screen-reader or WCAG certification.

Twenty-seven unchanged surfaces use fresh v1.256 production baselines: 54 raw main-DOM matches, 0 generated-ID-only matches, all54 exact measured geometries. The three delivered articles and news/guide libraries are included. The pre-existing /performance and /guides generated-ID comparator still requires unique/resolved references and rejects changed visible copy; no new normalization exception or geometry tolerance is added. Same-mode before/after capture avoids claiming a dev/prod ID difference as a UI change.

Final source gate after report/state: UI/Web/Portal/Ops typecheck; UI/Web lint; published-frame4 + article4 + reading6 + announcement7 component tests; next-route13 tests; clean-source current-state; git diff --check. Fifteen negative cases reject wrong article/section selection, rewritten original fields/date/related links, broken caller bindings/guide defaults/focus/reduced-motion and data intake. Exact final exits are in closure-checks.json. All294 runtime inputs are hashed and rechecked before commit and delivery. Code/browser review was inline; no independent reviewer-agent result is claimed.

## Coordination, delivery and limits

At preflight, v1.256 local/remote commit and previous package checksums matched. No pause, recovery, pending successor, new inbox, active batch or worker job blocked this request. The old v1.255 dashboard task remains REVIEW under operator authority; its scope/criteria/status are not repurposed for this separate direct v1.257 request.

The stored-PID capacity waiver remains with local command/own-PID journal. Fresh pause/recovery/ownership checks remain active. No Manager configuration/SQL state changes, secret/bridge identity reads, other-session claim release or process kill/restart. Branch and worktree remain unchanged.

Reviewed source/tests/docs only enter Git. Normal push, matching remote HEAD, full/delta/evidence ZIP integrity/SHA256, clean extracted archive validation and content-exact replay precede closure. Existing services/claims remain; new local loopback preview is3235, not a production deployment or background assistant work. Final registry state is WAITING_USER after verified handoff.

No invented publication/content, live feed, CMS, independent backend, production auth, DB persistence, personal data intake, game-server availability or download entitlement. NO_ACCEPTED_BACKEND_CONTRACT. Whole-app JavaScript-disabled streaming remains open; native controls do not prove universal no-JavaScript rendering.

Next after verified delivery only: WEB-FE-ACCESSIBILITY-INTERACTION-AUDIT-v1.258, `/news/news-guide-detail-pages-started`; not implemented here.
