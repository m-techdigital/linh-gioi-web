# HANDOFF — WEB-FE-START-VISUAL-REALIGNMENT-v1.270

`/start` has been rebuilt from the historical generic hero/proof flow into its page-specific immersive onboarding composition. Focused14/14 PASS, selected dev100/100 PASS, production100/100 PASS and build63 are recorded; BEFORE/MID/AFTER desktop/mobile screenshots were visually reviewed.

Authority is `PublicStartLanding` + `start-landing-layout.css` + shared `immersive-shell.css`. Five clean target-derived art crops are hash/provenance locked; labels, guide controls and actions remain live HTML. The historical technical onboarding screenshots are retained as evidence only, not runtime first-flow art.

Implementation source commit: `bc60189e5ec61fbb43bb8e0c3aca35f83684bbfc`, verified on `origin/main`. Historical Start board/gallery/target/layout suites v1.79/v1.90/v1.124/v1.139/v1.217 are superseded, not runtime PASS. Canonical/shared validators remain active.

No playable launch, account creation, saved tutorial progress, download entitlement, production auth, backend mutation or production deployment is claimed.

Next: `/download` visual realignment v1.271. Continue one page at a time; do not reopen `/start` without a related regression and do not resume the historical `/news` queue.
