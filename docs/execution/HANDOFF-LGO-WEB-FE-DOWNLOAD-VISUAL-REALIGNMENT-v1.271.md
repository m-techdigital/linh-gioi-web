# HANDOFF — WEB-FE-DOWNLOAD-VISUAL-REALIGNMENT-v1.271

`/download` has been rebuilt from the historical generic hero/proof-stack route into its page-specific immersive closed-release-gate composition. Focused14/14 PASS, clean selected dev114/114 PASS, production114/114 PASS and build63 are recorded; BEFORE/MID/AFTER desktop/mobile screenshots were visually reviewed.

Authority is `PublicDownloadLanding` + `download-landing-layout.css` + shared `immersive-shell.css`, with canonical `downloadReadiness` and `downloadBuilds` data. One clean target-derived locked-gate hero is hash/provenance locked; the whole design board is never embedded as runtime UI.

Implementation source commit: `dae79b11925fb330d7e89519b3e8ed2f9b26d823`, verified on `origin/main`. Historical `/download` v1.125/v1.140/v1.218 layout suites are superseded, not runtime PASS. Canonical content, public release truthfulness and navigation guards remain active.

The earlier 112/114 dev selected run was invalidated as harness evidence only: two `/game` cases failed during `browserContext.close` after a concurrent Playwright run removed shared trace resources. An isolated rerun with dedicated output and trace disabled passed 114/114; no product assertion was changed to obtain PASS.

No public binary, launcher, checksum artifact, entitlement, production account/auth, backend mutation or deployment is claimed.

Next: `/download/trust` visual realignment v1.272. Continue one page at a time; do not reopen `/download` without a related regression and do not resume the historical `/news` queue.
