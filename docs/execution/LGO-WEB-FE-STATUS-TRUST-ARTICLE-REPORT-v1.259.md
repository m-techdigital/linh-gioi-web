# WEB-FE-STATUS-TRUST-ARTICLE-v1.259

Status: WEB_VERIFY_PASSED. WEB_CLOSED becomes effective only after reviewed source commit, normal origin/main push with matching remote HEAD, full/delta/evidence ZIP integrity/SHA256 and exact replay are verified in external DELIVERY-CLOSURE.json.
Baseline: `38d63d8c1b08b8bba28f44aea2a358fa72e35f34`.
Session: `S-LGO-WEB-20260916-E6A1`. Same main and `/Users/minhdc/Projects/LinhGioiOnline-Web`.

## Real Browser UI/UX Layout First

Only `/news/status-download-trust-polish-started` is redesigned. Its previous production page clipped the introduction and chapter copy inside compact proof cards. It now consumes the existing illustrated editorial hero, original posting date, two complete parchment chapters, desktop contents column and mobile native contents disclosure. The existing Public Core palette/artwork/framing and unchanged shared shell are the comparison guardrail. Actual rendered HTML and screenshots were reviewed; a design image is not used in place of the interface and pixel-identical atlas artwork is not claimed.

The source title Copy trạng thái và download trust rõ hơn, summary, body, publishedAt and both contentDetailSections records are unchanged. Every chapter heading/body/playerImpact/nonClaim remains readable. The posting date05/09/2026 is formatted from its original timestamp using the shared vi-VN/UTC formatter. This historical WEB v1.10 article explains its original scope; it does not announce a live game build, current server health, entitlement or accepted backend. The historical game/runtime documents attached in the conversation do not replace the independent current Web source.

Seven contextual links open actual informational destinations. Chapter1 leads to Download conditions, Download Trust and Release Readiness. Chapter2 leads to Status, Release, Safety and FAQ. Each was opened by keyboard and returned from using Back in both browser profiles. These are not install commands, artifact links, sign-up forms or approval controls. Breadcrumb returns to News; three related articles preserve existing selection/order, original titles/summaries and actual article links.

Contents, previous/next and return-to-contents use the original shared fragment/focus behavior and actual section IDs. Direct URLs arrive below the sticky header. Back/Forward, invalid fragments, native disclosure, missing decorative artwork, reduced motion,320px source readability and44px actions are tested. Closing the contents does not hide chapters. Live resize including landscape and increased line/letter/word spacing preserves text and actionable onward links. In-page reading sends no fetch/XHR/non-GET request and changes no local/session storage.

## Base First and scope

PublicStatusTrustArticle is a thin source/label/link composition of unchanged PublishedArticle. Every packages file, CSS file, token, artwork/font, source fixture/contract, dependency/lockfile, shared shell and previously delivered article wrapper is unchanged. No duplicate article markup or navigation controller. The exact selected-slug branch follows the existing category/notFound guard and related selection. The generic return consumed by11 other news articles is byte-for-byte unchanged; their still-used CSS is not removed just to report cleanup.

Only the obsolete compact article v1.182 test/validator is HISTORICAL_SUPERSEDED; it is not counted as a passing runtime test. Other selected browser assertions are not relaxed. Source validation consumes the existing shared article contract plus this exact wrapper/source mapping and seven links.

## Runtime and visual evidence

The first implementation turn executed14 failing desktop cases on the old production page, then78/78 targeted dev cases including28 for this article. Component21/21 across four groups, next-route13/13, four typechecks, two lints and58 raw dev-DOM/geometry comparisons passed. That turn deliberately did not rebuild production while runtime reuse permission was pending. CHECKPOINT-DEV-VERIFIED.json and the original PROGRESS.md retain that incomplete checkpoint; they are history, not current closure.

At resume, all7 changed-file hashes still matched the dev-verified checkpoint, so UI was not rebuilt from scratch. The owner requested continuation after the explicit cleanup/reuse proposal. Runtime resources were consolidated first as detailed below. One new production build passed with63 static pages. The reused3236 preview was checked in a real browser: new article1, old compact0, two chapters, seven context links, HTTP200 and no page errors. It is not assumed that the old server hot-reloaded the new build.

Fresh selected production regression:742/742 PASS, zero failed/skipped/flaky across65 files. The selected article contributes28 tests. This is not reuse of v1.258's716 tests. All296 runtime input hashes are checked again before commit and delivery. No additional production build is needed for documentation-only closure changes.

| Production viewport | Height | Hero bottom | Article top | Related columns | Overflow | Main axe violations |
|---|---:|---:|---:|---:|---:|---:|
| 1440 × 900 | 2912 | 633.59 | 665.59 | 3 | 0 | 0 |
| 1280 × 800 | 2876 | 625.19 | 657.19 | 3 | 0 | 0 |
| 768 × 1024 | 3730 | 842.62 | 866.62 | 1 | 0 | 0 |
| 390 × 844 | 4943 | 1050.91 | 1074.91 | 1 | 0 | 0 |
| 360 × 800 | 5083 | 1076.83 | 1100.83 | 1 | 0 | 0 |

Default, full-page and chapter-arrival screenshots were reviewed on five production viewports. No horizontal/heading overflow, page JavaScript error, missing cover or main-content axe violation in selected tags was observed. This is Chromium viewport emulation with scoped axe, not physical-device/screen-reader testing or WCAG certification.

Twenty-nine unchanged surfaces use production-before screenshots/DOM saved from v1.258 before any source edit:58 exact raw main-DOM matches and58 exact measured geometries, with zero generated-ID-only exceptions needed. The five previously delivered article pages and news/guide libraries are included. Baseline files were not recaptured from the new source. Dev captures are archived separately so production screenshots are not mislabeled as dev evidence.

After report/state, the final source gate runs UI/Web/Portal/Ops typecheck; UI/Web lint; published-frame4, article4, reading6, announcement7 component tests; next-route13; clean-source current-state and git diff --check. Fifteen negative source cases reject wrong selection, rewritten source fields/date/related links, broken shared bindings/focus/reduced-motion and data intake. Exact command exits are closure-checks.json. Inline code/browser review only; no independent reviewer-agent result is claimed.

## Authorized runtime consolidation

The earlier owner message M-b25e3950abde required a read-only resource report before cleanup. The report found16 own listeners and14 finished handoff claims. It was completed and presented before the latest owner continuation request. That request followed the precise proposal to stop14 old Web previews3222–3235, retain dev3221, restart/reuse production3236 and release completed handoff write claims while preserving artifacts. RESUME-AUTHORIZATION.json records the actual request and bounded interpretation, not a fabricated Manager approval event.

Before any signal, every selected process was revalidated by listener PID, parent PID, start time, cwd, command, previous audit and own claims. Only exact verified Next Web listeners received SIGTERM; no global pkill, forced SIGKILL or process-group signal. All14 old previews and the prior3236 listener ended; all15 parent runners ended naturally. The14 now-unused port claims were released. All98 checksum entries across14 completed handoff directories were freshly matched and no open handles found before releasing only their own write claims. No ZIP, evidence, source or other-session resource was deleted or modified. The old v1.255 dashboard task remains under operator REVIEW.

The registry dropped from33 claims to5 retained resources: Web worktree, main branch, shared build runtime, dev3221 and reusable production3236. New production runs on3236, not a new version-number port. No change to Manager configuration, SQL state, bridge/client credentials, branch or worktree. Pause/recovery/ownership checks remain active. The local PID journal remains under the owner's stored-PID capacity waiver.

The resource inbox request is RESOLVED with the audit/cleanup evidence. The new handoff write claim is temporary and is released only after archive/SHA/replay verification; artifacts remain. Future page work must reuse these existing runtime roles, verify exact owner/identity before approved restart and avoid accumulating new preview ports. A listener left for user preview does not mean the assistant continues background work.

## Delivery and limits

Only reviewed source/tests/docs enter Git. Normal push, exact remote HEAD, verified full/delta/evidence archives, SHA256, clean extracted archive validator and exact baseline+delta replay precede closure. No handoff/cache/secret or font file enters source delivery. Final session state is WAITING_USER after verification, with only necessary long-lived runtime/worktree/branch claims retained.

No new live feed, CMS, independent backend, production auth, DB persistence, intake, release approval, download artifact or entitlement. NO_ACCEPTED_BACKEND_CONTRACT. No production deployment command. The local production-mode preview is not an Internet production release. Whole-app JavaScript-disabled streaming remains open; native controls do not establish universal no-JavaScript rendering.

Next after verified delivery only: WEB-FE-ACCESSIBILITY-INTERACTION-AUDIT-v1.260, `/news/closed-tester-information-pack-started`; not implemented here.
