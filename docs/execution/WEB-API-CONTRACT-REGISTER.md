# WEB-API-CONTRACT-REGISTER

No production web API contract accepted yet.

Portal and Ops real integration are blocked until Auth/DB/API/RBAC/audit contracts are accepted from the canonical Java/Spring Boot game backend.

Web consumes game backend contracts only through explicit sync.

No hand-written canonical DTO per app.

Fixture data is provisional and never canonical contracts.

Contract sync requires game source commit, API version, endpoint list, auth/session semantics, error semantics and unsupported fields.

Current status: WEB-08-GAME-CONTRACT-SYNC-v1.0 is blocked until accepted backend artifacts exist.
