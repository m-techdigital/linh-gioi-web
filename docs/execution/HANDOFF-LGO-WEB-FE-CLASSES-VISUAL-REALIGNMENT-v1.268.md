# HANDOFF — WEB-FE-CLASSES-VISUAL-REALIGNMENT-v1.268

`/classes` has been rebuilt from the historical generic hero/proof layout into its page-specific immersive five-Lộ composition. Focused14/14 PASS, selected dev74/74 PASS, production74/74 PASS and build63 are recorded; BEFORE/MID/AFTER desktop/mobile screenshots were visually reviewed.

Authority is `PublicClassesLanding` + `classes-landing-layout.css` + shared `immersive-shell.css`, with canonical `classPaths` data. Five clean character portraits plus one Võ feature crop are target-derived and hash/provenance locked; the hero reuses the already audited world artwork.

Implementation source commit: `00385ed64fe38140953580b5d3fce915a476323d`, verified on `origin/main`. Historical classes layout/art suites v1.54/v1.55/v1.122/v1.137/v1.215 are superseded, not runtime PASS. Canonical content and approved-art manifest guards remain active.

No registration, saved class selection, playable backend, download entitlement, login or production deployment is claimed.

Next: `/journey` visual realignment v1.269. Continue one page at a time; do not reopen `/classes` without a related regression and do not resume the historical `/news` queue.