# WEB-FE-CONTENT-HUB-ARTICLE-v1.262

Status: WEB_VERIFY_PASSED. WEB_CLOSED takes effect only after reviewed source commit, normal origin/main push with matching remote HEAD, full/delta/evidence ZIP integrity/SHA256, clean extracted source validation and exact baseline+delta replay in external DELIVERY-CLOSURE.json.
Baseline: `22d0d84b1a05eeae0c84a2ca8082136f1be256dc`. Session: `S-LGO-WEB-20260916-E6A1`.
Same main and `/Users/minhdc/Projects/LinhGioiOnline-Web`.

## Scope and real rendered interface

Only `/news/content-ia-hub-polish-started` is redesigned. Fresh before captures show an article with clipped introduction, summary, chapter bodies and outcomes inside compact proof cards. The implemented page now uses the shared illustrated publication header, source posting date, desktop contents rail and two complete parchment chapters. Mobile reflows to one column with native contents disclosure; closing contents never hides the article. Accepted Public Core palette/framing/artwork and the unchanged shared shell are comparison guardrails, not images embedded instead of real HTML. Actual desktop/mobile screenshots were viewed. Pixel-identical atlas artwork is not claimed.

The source title Content hub và lộ trình đọc được làm rõ, summary, body, publishedAt and both detail records remain unchanged. Every chapter heading/body/playerImpact/nonClaim is readable without line-clamping. Ngày đăng nội dung is05/09/2026, derived from the original timestamp by the unchanged vi-VN/UTC formatter. The original article describes WEB v1.12 at publication, not current game operations. The current /start route contains tutorial references and navigation; the article's historical hub description is neither silently rewritten nor treated as proof that an old personalized hub exists today. Historical game foundation/auth/database attachments do not determine this independent Web route.

Eight contextual links are organized by chapter. The first links to Start, the start-here cẩm nang, the Guides library and News. The second links to Game/World, Download conditions, Status and Community. Each opens an existing route and was activated by keyboard then returned from using Back in both browser profiles. These informational links do not trigger installation, account creation or permission changes. Three related articles preserve original selection/order, full summaries and their actual article URLs. Breadcrumb returns to News.

The existing contents, previous/next and return-to-contents behavior places focus on the actual section below the sticky header. Cold fragment URLs, Back/Forward and malformed/unknown fragments were tested. Missing decorative artwork leaves source text and keyboard navigation usable. Source chapter text at320px is at least14px and visible actions at least44px high. Normal focus, forced colors, reduced motion, live resize including landscape, and expanded line/letter/word spacing remain usable without clipping or duplicate contents navigation. In-page reading sends no fetch/XHR/non-GET request and writes no local/session storage.

## End-to-end reading paths, not duplicate tools

The article-to-Guides test opens the real library, selects Đọc website with Space, enters the exact source title in uppercase/unaccented form with surrounding spaces and verifies the actual one-result count and correct entry ID. It expands the complete source summary, opens the real start-here guide, goes Back and reloads; the local filter clears. Requests and browser storage are compared during filtering. The article does not contain a second search control, duplicate catalog, personalized ranking or account-aware recommendation service.

The article-to-News test filters the existing17-entry news catalog to this exact article, expands its original summary, opens the new article and verifies reload clears the search. No search submission is observed. The article-to-Start test verifies the current six tutorial labels, follows Tìm hiểu năm Lộ to /classes and returns using browser history. It does not assert that static tutorial illustrations are a playable game, nor rewrite the current Start page to match historical prose. These three real destination flows are covered on both profiles, six cases in addition to28 article cases.

Four additional full-page production captures show the filtered/expanded Guide and News destinations at desktop/mobile widths. Earlier viewport/element-region images are retained; a sticky header occluded the top of a region capture, so that partial image is not used as proof of the complete filter controls. The later full-page capture changes evidence framing only, not application behavior.

## Base First and ownership review

PublicContentHubArticle is only a typed source/label/eight-link composition of unchanged PublishedArticle. There is no new publication markup, controller or stylesheet. All packages, CSS, tokens, artwork/fonts, fixtures/contracts, dependency/lockfile, shared shell, Portal/Ops and previously delivered wrappers are unchanged. The selected-slug branch follows the existing category/notFound guard and related-source selection. The generic renderer still used by8 remaining source articles is byte-for-byte unchanged and its required CSS remains in place.

Only the obsolete compact v1.187 article layout test/validator is HISTORICAL_SUPERSEDED, not counted as runtime PASS. Existing source-content rules and all other selected browser assertions remain intact. New source validation checks the shared article contract and exact current wrapper/source labels/links. Review was inline source and browser inspection; no independent reviewer-agent result is claimed.

## Verification evidence

Preflight read current contract/CLI, active goal/state/ledger/roadmap, actual Git and registry. Local/remote baseline matched, tracked source was clean, seven v1.261 handoff checksums were freshly matched. There was no pause, recovery request or unknown worker job; the unrelated v1.255 dashboard task stayed operator REVIEW. No previous source was restored or old commit/push repeated.

RED:17 desktop cases failed on the old production page before UI implementation. GREEN:84/84 focused dev cases covering the new article, prior community article and Guides library passed. Component21/21 across four groups, next-route13/13, four typechecks/two lints and the clean-source dev gate passed. Seventy exact dev DOM/geometry comparisons passed. These dev results were not substituted for production evidence.

One fresh production build passed with63 static pages. After identity-checked reuse of3236, a browser probe confirmed HTTP200, new article1, old compact0, two chapters, eight context links and no page errors. All299 runtime input hashes are rechecked before commit and delivery. Dev captures are retained separately; closing after screenshots come from production.

The planned selected regression was enumerated before running:836 tests in64 files. Fresh production execution completed836/836 PASS with zero failed/skipped/flaky. This is a selected regression, not a claim about every historical repository test.34 cases cover the selected article, including six real destination flows. Commands, results, traces and screenshots are in handoff/continuous-v1.262/evidence.

| Production viewport | Page height | Hero bottom | Article top | Related columns | Overflow | Main axe violations |
|---|---:|---:|---:|---:|---:|---:|
| 1440 × 900 | 3034 | 657.91 | 689.91 | 3 | 0 | 0 |
| 1280 × 800 | 2996 | 649.50 | 681.50 | 3 | 0 | 0 |
| 768 × 1024 | 3857 | 866.94 | 890.94 | 1 | 0 | 0 |
| 390 × 844 | 5122 | 1050.91 | 1074.91 | 1 | 0 | 0 |
| 360 × 800 | 5245 | 1072.66 | 1096.66 | 1 | 0 | 0 |

No document/heading overflow, missing cover, page JavaScript error or scoped main-content axe violation was observed at five production viewports. These are Chromium viewport simulations with selected axe tags, not physical-device/screen-reader testing or WCAG certification.

Thirty-five unchanged surfaces have fresh v1.261 production baselines taken before edits. All70 raw main-DOM and measured geometry comparisons match, including delivered articles, the start-here cẩm nang and Guide/News libraries. The /start page itself is covered by unchanged-source verification and the new navigation test, not by these raw-DOM baseline pairs. No generated-ID exception was needed in this closing result. Existing narrow /performance and /guides ID comparators also test unique/resolved references and reject changed visible copy; no comparator or tolerance was loosened. Before images were not re-captured from new source.

After report/state, the final source gate checks UI/Web/Portal/Ops typecheck, UI/Web lint, published-frame4 + article4 + reading6 + announcement7 component tests, next-route13, clean-source current-state and git diff --check. Nineteen negative controls reject wrong article/content selection, rewritten fields/date/related links, broken shared bindings/focus/reduced-motion, data intake and altered Start/cẩm nang/Guide/News links. Actual exits are closure-checks.json. Source validation does not replace browser verification.

## Resource lifecycle and delivery

Only the existing5 long-lived claims and2 runtime roles are retained: worktree, main, build runtime, dev3221 and production3236. Before stopping old3236, PID/parent/start time/cwd/command were checked against preflight, its own previous launch journal and claim. Only that exact listener received SIGTERM; its parent ended naturally. Dev and other-session resources were untouched. New build uses the same3236 role; no per-version port accumulation, forced kill, process-group signal or Manager/configuration/SQL changes.

Pause/recovery/ownership checks remain active. The stored-PID capacity waiver uses local-process-journal.jsonl, not registry bypass through SQL. Temporary handoff write ownership is released only after archive validation and no open handles; ZIP/evidence are retained. A listener is a user preview, not continuing background assistant work.

Only reviewed source/tests/docs enter Git. Normal push with exact remote HEAD, full/delta/evidence ZIP without a parent wrapper, SHA256/integrity, clean archive validator and exact baseline+delta replay precede WEB_CLOSED. No local handoff/cache/secrets/fonts enter source delivery, no force-push and no production deployment. Final session state is WAITING_USER after verification.

No CMS, live feed, personal recommendation backend, public game artifact, account entitlement, production auth, DB persistence or independent business backend. NO_ACCEPTED_BACKEND_CONTRACT. Whole-app JavaScript-disabled streaming remains open; native disclosures do not prove universal no-JavaScript rendering.

Next after verified delivery only: WEB-FE-ACCESSIBILITY-INTERACTION-AUDIT-v1.263, `/news/world-gameplay-loop-depth-started`; not started here.
